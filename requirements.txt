# Whisperer Dependencies
# Install with: pip install -r requirements.txt

# CTranslate2-based Whisper implementation (much faster than openai-whisper)
faster-whisper>=1.0.0

# Audio recording
sounddevice>=0.4.6
numpy>=1.24.0

# Global hotkey listener
pynput>=1.7.6

# Typing output to active window
pyautogui>=0.9.54

# Clipboard support for Unicode text
pyperclip>=1.8.2

# Server (for Electron IPC)
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
websockets>=11.0
pydantic>=2.0.0
